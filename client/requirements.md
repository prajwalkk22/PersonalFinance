## Packages
recharts | For dashboard charts (pie, area, bar)
framer-motion | For smooth page transitions and micro-interactions
date-fns | For date formatting and manipulation
lucide-react | Icons (already in base, but confirming usage)
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging tailwind classes

## Notes
Authentication uses Replit Auth - strictly following the use-auth hook pattern.
Dashboard requires complex data visualization - Recharts is essential.
AI integration for categorization will be mocked on the frontend if the backend endpoint isn't ready, but wired to `api.ai.categorize` as specified.
Responsive design is critical for mobile usage.
